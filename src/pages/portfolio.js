import React from 'react';
import Navigation from '../components/Navigation';

const portfolio = () => {
    return (
        <div>
            <Navigation/>
            ce ci est le portfolio!!!
        </div>
    );
};

export default portfolio;